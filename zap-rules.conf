# ZAP Baseline Rule Configuration
# WARN-NEW alerts that are informational or non-actionable are set to IGNORE
# so they don't fail the pipeline.

# 10049 - Non-Storable Content: Static resources served with proper cache headers;
#         remaining hits are on dynamically secured API responses which intentionally
#         use no-store. This is expected behavior for a security-hardened app.
10049	IGNORE	(Non-Storable Content - expected for secured API responses)

# 10109 - Modern Web Application: Purely informational, ZAP noting
#         that the target is a JS-driven SPA. Not a vulnerability.
10109	IGNORE	(Modern Web Application - informational only)

# 10055 - CSP: style-src unsafe-inline: Required for inline styles to work.
#         Removing 'unsafe-inline' from style-src would break the UI.
#         This is an accepted, low-risk trade-off.
10055	IGNORE	(CSP style-src unsafe-inline - required for inline styles)
