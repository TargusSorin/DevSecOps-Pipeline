name: DevSecOps Pipeline

on:
  push:
    branches: ["main"]
    paths-ignore:
      - '**/*.md'
      - '**/*.txt'
  pull_request:
    branches: ["main"]
    paths-ignore:
      - '**/*.md'
      - '**/*.txt'
  workflow_dispatch:

permissions:
  contents: read

jobs:

  # Stage 1: Build & Test 
  build:
    name: Build & Test
    uses: ./.github/workflows/build.yml
    secrets: inherit

  # Stage 2: Security Checks

  # SCA 
  sca:
    name: Snyk SCA
    permissions:
      security-events: write
      contents: read
    uses: ./.github/workflows/sca.yml
    secrets: inherit

  # SAST
  sast:
    name: Comprehensive SAST
    permissions:
      security-events: write
      contents: read
    uses: ./.github/workflows/sast.yml
    secrets: inherit

  # Stage 3: Containerization, Scan & DAST
  container-dast:
    name: Container Build & DAST
    needs: [build, sca, sast]
    permissions:
      security-events: write
      contents: read
    uses: ./.github/workflows/container-dast.yml
    secrets: inherit

  # Secret Scanning – Gitleaks
  gitleaks:
    name: "Secret Scanning (Gitleaks)"
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Vulnerability & Misconfiguration Scanning – Trivy
  trivy-scan:
    name: "Vulnerability Scan (Trivy)"
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Trivy filesystem scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH'

      - name: Upload Trivy SARIF to GitHub Security
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: 'trivy-results.sarif'
          category: 'trivy'

      - name: Upload Trivy SARIF
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trivy-sarif
          path: trivy-results.sarif

  # Stage 4: Deployment
  deploy:
    name: Deploy to Azure
    needs: [container-dast]
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/deploy.yml
    secrets: inherit
